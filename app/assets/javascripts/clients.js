// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
// $(document).on('turbolinks:load', function() {

//   var client_search_input = $("#client-search-input");

//   client_search_input.attr('placeholder', 'Search clients...').focus();

//   client_search_input.on("keyup", function(e) {

//     var search_term = e.target.value.toLowerCase();
//     var $clients = $(".clients").find("tr");

//     $clients.hide().filter(function(idx) {
//         var el = $($clients[idx]),
//           anchor = el.find("a"),
//           name = anchor.text().toLowerCase();

//         return name.match(search_term);
//       })
//       .show();
//   });

// });
