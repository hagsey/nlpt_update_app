  // var app = angular.module('clients', []);

  // app.controller("ClientSearchController", [
  //   "$scope", "$http",
  //   function($scope, $http) {
  //     $scope.search = function(searchTerm) {
  //       if (searchTerm.length > 2) {

  //         $http.get("/clients.json", { "params": { "keywords": searchTerm } }).then(function(response) {
  //             $scope.clients = response.data;
  //           },
  //           function(response) {
  //             alert("There was a problem..." + response.status);
  //           }
  //         );
  //       }
  //     }
  //   }
  // ]);
